interface IProduct {
  productName: string;
  productPrice: number;
}

interface IProductResource {
  productName: string;
  productPrice: number;
  amount: number;
}

export { IProduct, IProductResource };
